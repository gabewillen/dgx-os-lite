# Root filesystem: XFS per project decision. mkosi copies the prepared
# rootfs tree in (CopyFiles with no source = the image root).
[Partition]
Type=root
Label=root
Format=xfs
CopyFiles=/
Minimize=guess
